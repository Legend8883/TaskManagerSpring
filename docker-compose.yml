services:
  postgres:
    image: postgres:16-alpine
    container_name: task_manager_postgres
    environment:
      POSTGRES_DB: TaskManager
      POSTGRES_USER: ${SPRING_DATASOURCE_USERNAME}
      POSTGRES_PASSWORD: ${SPRING_DATASOURCE_PASSWORD}
    ports:
      - "5433:5432"   # хост-порт 5433, т.к. 5432 занят локальным postgres
    volumes:
      - task_manager_postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${SPRING_DATASOURCE_USERNAME} -d TaskManager" ]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  task_manager_postgres_data: